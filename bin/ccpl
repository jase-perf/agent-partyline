#!/usr/bin/env bun
// ccpl — shim that invokes the TypeScript implementation.
// The logic lives in bin/ccpl.ts so tsc can typecheck it (tsc requires a
// .ts extension). Bun happily runs an extensionless file via this shebang.
import './ccpl.ts'
